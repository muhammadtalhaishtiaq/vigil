# Vigil — Python dependencies (single-process FastAPI app)

# ── Web app ───────────────────────────────────────────────────────────────
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
itsdangerous>=2.1,<3.0          # required by Starlette SessionMiddleware

# ── AI agents (any OpenAI-compatible endpoint; AIML API by default) ──────
openai>=1.30,<2.0

# ── Live market data (no API key needed for yfinance) ────────────────────
yfinance>=0.2.40,<0.3
requests>=2.31,<3.0

# ── Config ────────────────────────────────────────────────────────────────
python-dotenv>=1.0,<2.0
