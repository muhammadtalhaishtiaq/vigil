# Vigil — Python dependencies (console + MCP; framework-free agent engine)

# ── AI agents (any OpenAI-compatible endpoint; AIML API by default) ──────
openai>=1.30,<2.0

# ── MCP server (exposes the engine to Claude Desktop / Cursor / etc.) ────
mcp>=1.2,<2.0

# ── CLI (the live "watch the agents work" terminal view) ─────────────────
rich>=13,<14

# ── Live market data (no API key needed for yfinance) ────────────────────
yfinance>=0.2.40,<0.3
requests>=2.31,<3.0

# ── Config ────────────────────────────────────────────────────────────────
python-dotenv>=1.0,<2.0
